//this is to sync the cards ALL LOADING

export default function LoadAll({
    catOptions,
}){
    const max = catOptions.length()
    const [loadCount,setLoadCount] = useState(0)

    function handle(){
        
    }
}